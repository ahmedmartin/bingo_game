<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ahmed.martin.bingo_game">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sighn_up" />
        <activity android:name=".Main" />
        <activity android:name=".join" />
        <activity android:name=".resault" />
        <activity android:name=".game" />

        <meta-data
            android:name="com.google.android.gms.ads.com.ahmed.martin.bingo_game"
            android:value="ca-app-pub-9940348601431996~3658002235" />

        <activity android:name=".sighn_in" />
        <activity android:name=".about_us"></activity>
    </application>

</manifest>